
//extern  DWORD ErrorCount;
//extern  DWORD WarningCount;

typedef enum { WARNING0, WARNING1, ERROR0, ERROR1, FATAL } TErrorType;

void Error(TErrorType e, const char *pbFormat, ...);

#define ASSERT(expression) if (!(expression)) { Error(FATAL, "\n    Source:    %s\n    Line:      %d.\n    Function:  %s\n    Condition: (%s)\n", __FILE__, __LINE__, __FUNCTION__, #expression ); }

/* eof */

